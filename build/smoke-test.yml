resources:
- repo: self

jobs:

- template: smoke-test-template.yml
  parameters:
    agentSpecification: 'windows-2019'

- template: smoke-test-template.yml
  parameters:
    agentSpecification: 'vs2017-win2016'